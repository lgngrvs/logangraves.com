FLASK_ENV=production
DEBUG=False
# SERVER_NAME should be set in the actual production environment (e.g. Heroku config vars),
# not here — this file is loaded locally too, which breaks subdomain routing in dev.
# Production: SERVER_NAME=logangraves.com